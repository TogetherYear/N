{
  "name": "node-empty",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "tsc && esbuild dist/index.js --bundle --minify --outfile=build/bound.js --platform=node"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "esbuild": "^0.21.5",
    "typescript": "^5.3.3"
  }
}